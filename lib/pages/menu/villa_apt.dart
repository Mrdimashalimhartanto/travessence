part of 'menu.dart';

class VillaApt extends StatelessWidget {
  const VillaApt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('Halaman Villa & Apt')));
  }
}
